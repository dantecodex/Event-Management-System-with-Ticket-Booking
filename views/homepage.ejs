<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="shortcut icon" href="#">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/homepage.css">
    <title>Events</title>
</head>

<body>
    <!-- Navbar -->
    <nav class="navbar navbar-dark bg-dark navbar-expand-lg">
        <div class="container-fluid mx-3">
            <div class="">
                <a class="navbar-brand" href="/"><img src="/assets/event-master-high-resolution-logo-transparent.png"
                        alt="logo" srcset="" width="230px"></a>
            </div>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false"
                aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                    <li class="nav-item ms-2">
                        <a class="nav-link active" aria-current="page" href="/homepage/<%= user._id %>">Home</a>
                    </li>
                    <li class="nav-item ms-2">
                        <a class="nav-link active" aria-current="page" href="/homepage/<%= user._id %>/my-event">My
                            Events</a>
                    </li>
                    <li class="nav-item ms-2">
                        <a class="nav-link active" aria-current="page" href="#">About</a>
                    </li>
                    <li class="nav-item ms-3">
                        <img src="https://mdbcdn.b-cdn.net/img/new/avatars/1.webp" class="rounded-circle"
                            style="width: 40px;" alt="Avatar" />
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="#">
                            <%= user.name %>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="/api/user/logout">
                            Logout
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Event card -->
    <div class="mt-md-5 mt-4">

        <div class="container d-flex justify-content-between">
            <div>
                <p class="h4" style="font-family: Cormorant Garamond">Upcoming Events</p>
            </div>
            <div class="">
                <button class="btn btn-dark shadow"
                    onclick="window.location.href='/homepage/<%= user._id %>/create-event'">Create Event</button>
            </div>
        </div>
    </div>

    <div class=" container d-flex flex-wrap" id="all-events">
        <% if(events.length){ %>
            <% for( let index=0; index < (events.length> 4 ? 4:events.length); index++ ) { %>
                <div class="card my-3 text-dark bg-light m-auto" style="width: 18rem; cursor: pointer;"
                    onclick="window.location.href= '/homepage/<%= user._id %>/event/<%= events[index]._id %>'">
                    <div class="card-body">
                        <h5 class="card-title">
                            <%= events[index].name %>
                        </h5>
                        <p class="card-text text-secondary m-0 mb-1">
                            <%= events[index].location %>
                        </p>
                        <p class="card-text text-secondary m-0 mb-1">
                            <%= events[index].date %>
                        </p>
                        <p class="card-text m-0 mb-1"><i class="bi bi-currency-dollar"></i>
                            <%= events[index].price %>
                        </p>
                        <p class="card-text"><i class="bi bi-people-fill"></i>
                            <%= events[index].participants %>
                        </p>
                    </div>
                </div>
                <% } %>

                    <% } else{ %>
                        <p class="h1 mt-5">There is Upcoming Events. You can create one 😉 </p>
                        <% } %>
    </div>
    <% if(events.length){ %>
        <div class="text-center mt-md-4 mt-3">
            <button class="btn btn-dark shadow" onclick="window.location.href='/homepage/<%= user._id %>/browse'">Browse
                Events</button>
        </div>

        <% } %>






















            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
                integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
                crossorigin="anonymous"></script>
</body>

</html>